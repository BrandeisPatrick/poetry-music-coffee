{
  "buildCommand": "cp deployment/frontend/package.frontend.json package.json && npm install && npm run build",
  "outputDirectory": "dist",
  "installCommand": "cp deployment/frontend/package.frontend.json package.json && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}